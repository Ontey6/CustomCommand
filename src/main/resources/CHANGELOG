# Version 0.5.1

now broadcasts updater message to OP's on join - add toggle!

fixed escaping of advanced colors

made legacy colors escapable with \&e too rather than just &&e

added mini-message abbreviations

allow spaces in <r,g,b>: <_r,_g,_b_> (underscores are possible spaces)